{
  "name": "Bigscreen",
  "version": "0.0.1",
  "main": "src/main.js",
  "license": "MIT",
  "scripts": {
    "serve": "NODE_ENV=development electron .",
    "build": "node build.js",
    "lint": "eslint --ext .js ./",
    "lint:fix": "eslint --fix .js ./",
    "test": "NODE_ENV=test mocha $(find . -name '*_spec.js' ! -ipath '*node_modules*')",
    "test:coverage": "",
    "test:watch": ""
  },
  "dependencies": {
    "electron": "^1.6.2",
    "electron-sudo": "^4.0.12"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "commander": "^2.9.0",
    "electron-packager": "^8.6.0",
    "eslint": "^3.18.0",
    "mocha": "^3.2.0",
    "sinon": "^2.1.0"
  }
}
